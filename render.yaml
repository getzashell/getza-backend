services:
  - type: web
    name: getza-backend
    env: node
    region: oregon
    plan: free
    autoDeploy: "no"
    healthCheckPath: /healthz
    buildCommand: echo "Built"
    startCommand: node -e "console.log('Starting...'); setInterval(()=>{}, 100000)"
