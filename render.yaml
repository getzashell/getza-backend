services:
  - type: web
    name: getza-backend
    env: node
    region: oregon
    plan: free
    buildCommand: echo "Built"
    startCommand: node -e "console.log('Node works'); setTimeout(()=>{}, 120000)"
