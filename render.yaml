services:
  - type: web
    name: getza-backend
    env: docker
    dockerfilePath: ./Dockerfile
    region: frankfurt
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
